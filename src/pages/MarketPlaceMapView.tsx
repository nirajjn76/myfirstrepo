import React from 'react';
import MapViewComponent from '../components/MapviewMarketplace';

const MapView: React.FC = () => {
  return (
    <div className="mapview-marketplace-root">
      <MapViewComponent />
    </div>
  );
};

export default MapView;
